declare class RabbitMQ {
    private static instances;
    private connection;
    private channel;
    private exchange;
    private constructor();
    static getInstance(exchange: string): RabbitMQ;
    connect(): Promise<void>;
    publish(exchange: string, routingKey: string, msg: any): Promise<void>;
    requestReply(exchange: string, routingKey: string, msg: any): Promise<any>;
    consume(queue: string, bindingKey: string, callback: (msg: any) => void): Promise<void>;
    handleRequest(queue: string, bindingKey: string, processFunction: (msg: any) => any): Promise<void>;
    private generateUuid;
    close(): Promise<void>;
}
export default RabbitMQ;
