"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Publisher = void 0;
class Publisher {
    constructor(channel) {
        this.channel = channel;
    }
    publish(data, exchangeType = 'direct') {
        return new Promise((resolve, reject) => {
            this.channel.assertExchange(this.exchange, exchangeType, { durable: true });
            const success = this.channel.publish(this.exchange, // Exchange: Sử dụng '' nếu bạn sử dụng default exchange
            this.routingKey, Buffer.from(JSON.stringify(data)));
            if (success) {
                console.log('Event published to routingKey', this.routingKey);
                resolve();
            }
            else {
                reject(new Error('Failed to publish message'));
            }
        });
    }
}
exports.Publisher = Publisher;
