import Redis from 'ioredis';
export declare class RedisClient {
    private static instance;
    constructor();
    static getInstance(): Redis;
    static set(key: string, value: string | object, expireInSeconds?: number): Promise<string | null>;
    static get<T = string | object>(key: string): Promise<T | null>;
    static delete(key: string): Promise<number>;
    static exists(key: string): Promise<boolean>;
    static expire(key: string, seconds: number): Promise<boolean>;
    static ttl(key: string): Promise<number>;
}
